# Developing etcd-druid locally

You can setup `etcd-druid` locally by following detailed instructions in [this document](../deployment/getting-started-locally/getting-started-locally.md). For best development experience you should use `make deploy-dev`. In case you wish to start a debugging session then use `make deploy-debug`.